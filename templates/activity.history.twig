{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="card">
            <div class="row">
                <div class="text-center">
                    <span class="numbers btn btn-info btn-simple">Activity History</span>
                </div>
            </div>
            <div class="card-content" id="powerbi_dashboard_container">
                {%  if account.historyactivityurl is empty %}
                    {{ activity_history_dashboardUrl |raw }}
                {%  else %}
                    {{ account.historyactivityurl |iframecontent |raw }}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock body %}