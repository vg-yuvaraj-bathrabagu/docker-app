<div class="form-group">
    <label class="control-label">Title : </label>
    <span class="help-block">{{ taskcategory.title }}</span>
</div>
<div class="form-group">
    <label class="control-label">Description: </label>
    <span class="help-block">{{ taskcategory.description }}</span>
</div>