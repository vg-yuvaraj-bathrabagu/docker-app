<div class="form-group">
    <label class="control-label">Title : </label>
    <span class="help-block">{{ project.title }}</span>
</div>
<div class="form-group">
    <label class="control-label">Project ID : </label>
    <span class="help-block">{{ project.projectid }}</span>
</div>
<div class="form-group">
    <label class="control-label">Cost Center Code : </label>
    <span class="help-block">{{ project.costcentercode }}</span>
</div>
<div class="form-group">
    <label class="control-label">Activity : </label>
    <span class="help-block">{{ project.activity }}</span>
</div>
<div class="form-group">
    <label required class="control-label">Labor PO Line : </label>
    <span class="help-block">{{ project.laborpoline }}</span>
</div>
<div class="form-group">
    <label required class="control-label">ODC PO Line : </label>
    <span class="help-block">{{ project.odcpoline }}</span>
</div>
<div class="form-group">
    <label required class="control-label">Travel PO Line : </label>
    <span class="help-block">{{ project.travelpoline }}</span>
</div>
<div class="form-group">
    <label required class="control-label">Type : </label>
    <span class="help-block">{{ project.type }}</span>
</div>
<div class="form-group">
    <label required class="control-label">Number of Sprints : </label>
    <span class="help-block">{{ project.sprintcount }}</span>
</div>
<div class="form-group">
    <label required class="control-label">Length of Sprints (Weeks) : </label>
    <span class="help-block">{{ project.sprintduration }}</span>
</div>
