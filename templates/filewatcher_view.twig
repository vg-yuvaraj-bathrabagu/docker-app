<div class="form-group">
    <label class="control-label">Title : </label>
    <span class="help-block">{{ filewatcher.title }}</span>
</div>
<div class="form-group">
    <label class="control-label">Type : </label>
    <span class="help-block">{{ filewatcher.category }}</span>
</div>
<div class="form-group">
    <label class="control-label">Folder : </label>
    <span class="help-block">{{ filewatcher.bucket }}</span>
</div>
<div class="form-group">
    <label class="control-label">Description : </label>
    <span class="help-block">{{ filewatcher.description }}</span>
</div>
<div class="form-group">
    <label required class="control-label">Report Code : </label>
    <span class="help-block">{{ filewatcher.querystring }}</span>
</div>
<div class="form-group">
    <label required class="control-label">Security Policy : </label>
    <span class="help-block">{{ filewatcher.securitypolicy }}</span>
</div>